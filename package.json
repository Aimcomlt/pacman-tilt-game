{
  "name": "pacman-tilt-game",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "ci": "npm run lint --if-present --workspaces && npm run test --if-present --workspaces && npm run typecheck --if-present --workspaces && npm run build --if-present --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
