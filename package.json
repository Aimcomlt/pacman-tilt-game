{
  "name": "drone-invaders-neural-assistance",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "echo 'Pac-Man tests removed in cleanup pass'",
    "ci": "npm run lint --if-present --workspaces && npm run typecheck --if-present --workspaces && npm run build --if-present --workspaces"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "typescript": "^5.4.5"
  }
}
