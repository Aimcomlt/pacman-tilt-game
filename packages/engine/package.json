{
  "name": "@pacman/engine",
  "version": "0.1.0",
  "main": "src/index.ts",
  "private": true,
  "scripts": {
    "build": "tsc -p . --emitDeclarationOnly false",
    "typecheck": "tsc -p . --noEmit",
    "lint": "eslint src",
    "test": "jest"
  },
  "dependencies": {
    "@pacman/shared": "0.1.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.3.4"
  }
}
